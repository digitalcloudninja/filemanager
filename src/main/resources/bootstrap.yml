spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: configuration
      name: filemanager
eureka:
  client:
    region: us-east-1
    registry-fetch-interval-seconds: 30
  instance:
    prefer-ip-address: true